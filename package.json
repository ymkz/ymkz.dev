{
  "name": "ymkz.dev",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "wrangler pages dev ./public --port=3000",
    "deploy": "wrangler pages publish ./public",
    "lint": "biome check",
    "format": "biome check --write"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "wrangler": "3.103.2"
  }
}
