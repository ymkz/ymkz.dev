{
  "private": true,
  "scripts": {
    "deploy": "wrangler pages publish ./static",
    "dev": "wrangler pages dev ./static --port=3000",
    "fmt": "prettier --write .",
    "lint": "prettier --check ."
  },
  "prettier": {
    "semi": false,
    "singleQuote": true
  },
  "devDependencies": {
    "prettier": "2.8.8",
    "wrangler": "3.1.1"
  }
}
