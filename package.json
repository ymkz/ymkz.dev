{
  "private": true,
  "scripts": {
    "build": "node script/build",
    "deploy": "wrangler publish --env production",
    "dev": "miniflare --live-reload --watch",
    "fmt": "prettier --write .",
    "lint": "tsc --noEmit && prettier --check ."
  },
  "prettier": {
    "semi": false,
    "singleQuote": true
  },
  "dependencies": {
    "hono": "0.5.0",
    "nano-jsx": "0.0.29"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "3.4.0",
    "esbuild": "0.14.25",
    "miniflare": "2.3.0",
    "prettier": "2.5.1",
    "prettier-plugin-packagejson": "2.2.15",
    "typescript": "4.6.2"
  }
}
