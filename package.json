{
  "private": true,
  "scripts": {
    "deploy": "wrangler pages publish ./static",
    "dev": "wrangler pages dev ./static --port=3000",
    "fmt": "prettier --write .",
    "lint": "prettier --check ."
  },
  "prettier": {
    "semi": false,
    "singleQuote": true
  },
  "devDependencies": {
    "prettier": "3.0.1",
    "wrangler": "3.4.0"
  }
}
