{
  "name": "ymkz.dev",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev ./public --port=3000",
    "lint": "prettier --check .",
    "lint:fix": "prettier --write .",
    "deploy": "wrangler pages publish ./public"
  },
  "devDependencies": {
    "prettier": "3.2.4",
    "wrangler": "3.23.0"
  }
}
