{
  "name": "ymkz.dev",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev ./static --port=3000",
    "lint": "prettier --check .",
    "lint:fix": "prettier --write .",
    "deploy": "wrangler pages publish ./static"
  },
  "devDependencies": {
    "prettier": "3.0.3",
    "wrangler": "3.13.1"
  }
}
